Task 12 reviewer verified MapBloc subscribes to ActiveRegionBloc.stream in constructor, forwards via MapViewportChanged event, and overrides close() to cancel the subscription. Task 13 reporter confirms AircraftListBloc follows identical pattern: subscribes to ActiveRegionBloc.stream, forwards via AircraftListRegionUpdated, cancels in close(). Both use BlocProvider(create: (_) => getIt<MapBloc>()) which auto-calls bloc.close() on page disposal per flutter_bloc-9.1.1 source. Reviewer verified no leak because BlocProvider's create: constructor (not .value) triggers auto-disposal lifecycle.