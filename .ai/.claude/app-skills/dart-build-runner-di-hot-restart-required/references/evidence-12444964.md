It's written — `_LiveUpdatesBlock` in `settings_page.dart`, wired up with the Save button. It won't show via hot reload though: I regenerated `injection.config.dart` because `SettingsBloc`'s constructor signature changed (dropped the now-unused `AircraftRepository` param). Hot reload can't re-run dependency injection wiring — that needs a hot **restart** (capital R in your terminal, or stop/relaunch), not a reload.