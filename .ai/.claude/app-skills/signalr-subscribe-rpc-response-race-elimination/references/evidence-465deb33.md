Fixed and verified — 40/40 backend, 63/63 frontend, both clean. That race was the real reason the region-reset fix didn't feel like it took effect: the server was resetting the clock and pushing correctly, but the client dropped the push and silently fell back to waiting for the next tick. Now `Subscribe` returns the snapshot as its own RPC response, so there's no window where it can be dropped.