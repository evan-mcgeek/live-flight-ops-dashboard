The `/understand` skill hardcodes its output to `<PROJECT_ROOT>/.understand-anything/`. Since I scoped the *analysis* to `api/` per your instruction, I set `PROJECT_ROOT=api/`, and the skill mechanically wrote `.understand-anything/` there. But your very first brainstorming message named `.unserstand-anything` explicitly as an example of what belongs in `.ai/` — so this output directory is in the wrong place structurally, even though the analysis scope (api/ only) is correct. The tool doesn't have a flag to separate "where to scan" from "where to write output" — those are both driven by the same `PROJECT_ROOT`. [Solution:] # --- Scope: backend (api/) only for this pass --- .ai/ app/